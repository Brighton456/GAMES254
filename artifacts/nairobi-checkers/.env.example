# Games254 client Supabase wiring (optional; graceful fallback without it).
# Copy to .env (never commit) — the local cash balance keeps working from
# localStorage until these are present.
VITE_SUPABASE_URL=https://aqsvdmramlaxuwpmaxzu.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx

# Vite needs these for any build on this box:
# PORT=5001
# BASE_PATH=/